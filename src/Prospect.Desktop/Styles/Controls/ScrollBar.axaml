<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- Port de .pr-scroll (tokens/base.css) : fine (10px), pouce seul, pas de flèches,
       pilule, stone-700 au repos et stone-600 au survol (ScrollbarThumb / ScrollbarThumbHover,
       déjà des alias sémantiques de Colors.axaml).

       La gouttière qui écarte la barre du bord de sa fenêtre vit à côté, dans
       Styles/Controls/ScrollViewer.axaml : elle ne concerne que les barres appartenant à un
       ScrollViewer, pas une ScrollBar posée à la main. -->

  <ControlTheme x:Key="{x:Type ScrollBar}" TargetType="ScrollBar">
    <Setter Property="Cursor" Value="Arrow" />

    <Style Selector="^:horizontal">
      <Setter Property="Height" Value="{StaticResource ScrollbarW}" />
      <Setter Property="MinHeight" Value="{StaticResource ScrollbarW}" />
      <Setter Property="Template">
        <ControlTemplate>
          <Track Orientation="Horizontal"
                 Minimum="{TemplateBinding Minimum}"
                 Maximum="{TemplateBinding Maximum}"
                 Value="{TemplateBinding Value, Mode=TwoWay}"
                 ViewportSize="{TemplateBinding ViewportSize}">
            <Thumb Name="thumb" />
          </Track>
        </ControlTemplate>
      </Setter>
    </Style>

    <Style Selector="^:vertical">
      <Setter Property="Width" Value="{StaticResource ScrollbarW}" />
      <Setter Property="MinWidth" Value="{StaticResource ScrollbarW}" />
      <Setter Property="Template">
        <ControlTemplate>
          <Track Orientation="Vertical"
                 IsDirectionReversed="True"
                 Minimum="{TemplateBinding Minimum}"
                 Maximum="{TemplateBinding Maximum}"
                 Value="{TemplateBinding Value, Mode=TwoWay}"
                 ViewportSize="{TemplateBinding ViewportSize}">
            <Thumb Name="thumb" />
          </Track>
        </ControlTemplate>
      </Setter>
    </Style>

    <Style Selector="^ /template/ Thumb#thumb">
      <Setter Property="Background" Value="{DynamicResource ScrollbarThumb}" />
      <Setter Property="Template">
        <ControlTemplate>
          <Border Background="{TemplateBinding Background}"
                  CornerRadius="{StaticResource RadiusPill}"
                  Margin="2">
            <Border.Transitions>
              <Transitions>
                <BrushTransition Property="Background" Duration="{StaticResource DurInstant}" Easing="{StaticResource EaseStandard}" />
              </Transitions>
            </Border.Transitions>
          </Border>
        </ControlTemplate>
      </Setter>
    </Style>
    <Style Selector="^ /template/ Thumb#thumb:pointerover">
      <Setter Property="Background" Value="{DynamicResource ScrollbarThumbHover}" />
    </Style>
  </ControlTheme>

</ResourceDictionary>
